# Railway PostgreSQL Environment Variables Template
# Copy these values from your Railway PostgreSQL service

# Railway provides these automatically when you add PostgreSQL service:
DATABASE_URL=${PGDATABASE_URL}

# Or you can construct it manually:
# DATABASE_URL=postgresql://username:password@host:port/database

# Other environment variables
PYTHONPATH=/app/backend
PORT=8000

# CORS origins for frontend (update with your Railway frontend URL)
CORS_ORIGINS=https://your-frontend.railway.app,http://localhost:3000

# Optional: API keys for stock price services
# ALPHA_VANTAGE_API_KEY=your_key_here
# YAHOO_FINANCE_API_KEY=your_key_here