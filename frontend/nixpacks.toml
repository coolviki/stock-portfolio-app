[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npx serve -s build -l $PORT"

[variables]
NODE_ENV = "production"
GENERATE_SOURCEMAP = "false"
CI = "false"
PORT = "3000"